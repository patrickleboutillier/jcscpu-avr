---

header: "class jand3 { public: jand3(input wa, input wb, input wc, output wd) ; } ;"

source: |
  jand3::jand3(input wa, input wb, input wc, output wd){
    wire os ;
    jand and0(wa, wb, os) ;
    jand and1(wc, os, wd) ;
  }

test: |
  wire a("a"), b("b"), c("c"), d("d") ;
  jand3 x(a, b, c, d) ;

rank: 4

testperl: |
  use strict ;
  require test ;

  test('jand3',
     [qw(a b c)],
     [qw(d)]) ;

testdata: |
  000=0
  001=0
  010=0
  011=0
  100=0
  101=0
  110=0
  111=1
